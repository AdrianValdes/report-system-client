import React from 'react';
import { Header, HeaderStyles } from './header.style';

export const Navbar = () => (
  <HeaderStyles>
    <Header>
      <p>Logo</p>
      <p>Reports</p>
    </Header>
  </HeaderStyles>
);
